interface:
  display_name: "BeUniq"
  short_description: "Rule-based frontend design context, component-style selection, AI-slop, copy, taste, and motion checker."
  default_prompt: "Use BeUniq to initialize PRODUCT.md/DESIGN.md if needed, choose a component style profile from this frontend's component collection, audit for AI-slop, copy slop, taste, and motion craft, then apply targeted fixes until it passes."
